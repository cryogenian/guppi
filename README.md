+ Install stuff for iOS development (XCode, XCode cli tools, etc)
+ Install stuff for Android development
+ `npm install -g cordova`
+ `npm install -g pulp`
+ `npm install`
+ `bower install`
+ `pulp browserify -t www/js/index.js && cordova emulate ios`
